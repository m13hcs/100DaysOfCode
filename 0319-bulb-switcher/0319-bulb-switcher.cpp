class Solution {
public:
    int bulbSwitch(int n) {
        /*
        Only the bulbs at square positions will be left on as they have
        odd number of prime factories, implying that the last switch will 
        turn the bulb on and there exists sqrt(n) squares in n integers
        */
        
        return sqrt(n);
       
    }
};
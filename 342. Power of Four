class Solution {
public:
    bool isPowerOfFour(int n) {
        if(n == 0)
        {
            return false; 
        }

        double num = log(n)/log(4);

        if(ceil(num)==floor(num))
        {
            return true; 
        }

        return false; 
    }
};
